groups:
  - name: taskflow-alerts
    rules:
      - alert: TaskFlowBackendDown
        expr: up{job="taskflow-backend"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "TaskFlow backend is down"
          description: "Prometheus cannot scrape the TaskFlow backend metrics endpoint."

