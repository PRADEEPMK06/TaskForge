apiVersion: v1
kind: ConfigMap
metadata:
  name: taskflow-config
  namespace: taskflow
data:
  DATABASE_URL: "sqlite:///./data/taskflow.db"
  ACCESS_TOKEN_EXPIRE_MINUTES: "720"
  CORS_ORIGINS: "http://localhost:3000,http://taskflow.local"

