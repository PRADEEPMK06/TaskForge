apiVersion: v1
kind: ConfigMap
metadata:
  name: taskforge-config
  namespace: taskforge
data:
  DATABASE_URL: "sqlite:///./data/taskforge.db"
  ACCESS_TOKEN_EXPIRE_MINUTES: "720"
  CORS_ORIGINS: "http://localhost:3000,http://taskforge.local"

